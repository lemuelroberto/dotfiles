#!/usr/bin/env bash

set -euEo pipefail
trap 'echo "${BASH_SOURCE:-unknown}:${LINENO:-unknown}: $BASH_COMMAND";' ERR

sleep '15m' && paplay /usr/share/sounds/freedesktop/stereo/alarm-clock-elapsed.oga
